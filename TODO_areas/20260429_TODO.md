# TODO: Measurement Pipeline Refinement

This list outlines the necessary technical debt and structural improvements required to move this script from a researcher's prototype to a robust analysis tool.

## 🔴 High Priority: Correctness & Stability
- [ ] **Fix Silent Failure in `utils._safe_float`**: Change the `try-except` block to raise a `ValueError` with a descriptive message (e.g., including the string that failed to parse) instead of returning `NaN`.
- [ ] **Robust CSV Parsing**: Update `load_measurement_csv` in `loaders.py` to handle different delimiters. Use `pd.read_csv(filepath, sep=None, engine='python')` to allow automatic detection of tabs, commas, or semicolons.
- [ ] **Filename Validation**: Add explicit error handling for the timestamp and field splitting in `load_metadata_csv`. If a filename doesn't match the expected `YYYYMMDD_HHMMSS_...` pattern, it should raise a clear error identifying the bad file.

## 🟡 Medium Priority: Design & Architecture
- [ ] **Decouple Data Loading from Analysis**: Remove the call to `processes.linear_fit` inside `load_dataset_csv`. The loader should return data; the user/main script should decide when and how to run the fit.
- [ ] **Refactor Summary Generation**: 
    - [ ] Add a `.to_dict()` or `.get_summary_data()` method to the `Dataset` class.
    - [ ] Update `DatasetCollection.summary_df` to iterate through datasets and call this method, removing the hardcoded dictionary keys from the collection class.
- [ ] **Memory Optimization**: Audit the `Measurement` dataclass in `models.py`. Remove redundant `.copy()` calls in `from_dataframe` and `to_dataframe` unless explicit protection from external mutation is strictly required for the workflow.

## 🟢 Low Priority: Style & Standardisation
- [ ] **Entry Point Protection**: Wrap the execution logic in `main.py` inside a `def main():` function and add the standard `if __name__ == "__main__":` guard.
- [ ] **Complete Type Hinting**: 
    - [ ] Add return type hints to `__iter__` and `__len__` in `DatasetCollection`.
    - [ ] Add full type annotations to `processes.linear_fit`.
- [ ] **Constant Consistency**: Update `constants.py` so that `ColumnNames.required()`, `ColumnNames.all()`, and other helper methods return the same collection type (preferably `set` for validation and `list` for ordered access).
- [ ] **Docstring Audit**: Ensure all methods in `models.py` and `processes.py` have docstrings following the same style as `loaders.py`.